// Copy to Secrets.xcconfig (git-ignored) and fill in.
// Host only, no scheme: "//" starts a comment in xcconfig files.
SUPABASE_HOST = your-project-ref.supabase.co
SUPABASE_ANON_KEY = your-anon-key
